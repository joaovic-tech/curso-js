// Tipos de dados primitivos String, Number, undefined, null,boolean, symbol EXEMPLOS:
const nome = 'João'; // String
const num = 10; // Number
let nomeAluno; // undefined -> (indefinido) não aponta pra local nenhum na memória 
let sobrenomeAluno = null; // Nulo -> (nulo ,ou seja, Sem valor) não aponta pra local nenhum na memória 
const aprovado = true; // Boolean -> valor lógico verdadeiros(true) ou falsos(false)